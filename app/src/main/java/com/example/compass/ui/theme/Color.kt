package com.example.compass.ui.theme

import androidx.compose.ui.graphics.Color

// === DARK THEME (Primary - Matching Design) ===
val DarkBackground = Color(0xFF0D1117)          // Deep dark background
val DarkSurface = Color(0xFF161B22)             // Slightly lighter surface
val DarkCard = Color(0xFF21262D)                // Card background
val DarkCardElevated = Color(0xFF2D333B)        // Elevated card
val DarkBorder = Color(0xFF30363D)              // Border color

// === LIGHT THEME ===
val LightBackground = Color(0xFFF5F3EE)         // Cream background
val LightSurface = Color(0xFFFFFFFF)            // White surface
val LightCard = Color(0xFFFFFFFF)               // White card
val LightCardElevated = Color(0xFFFAFAFA)       // Slightly gray card
val LightBorder = Color(0xFFE0E0E0)             // Light border

// === ACCENT COLORS ===
val LimeAccent = Color(0xFFCDFF00)              // Bright lime/chartreuse
val LimeAccentDark = Color(0xFFB8E600)          // Darker lime
val CyanAccent = Color(0xFF00D4FF)              // Bright cyan for highlights
val BlueAccent = Color(0xFF58A6FF)              // Blue accent

// === TEXT COLORS ===
val TextPrimary = Color(0xFFFFFFFF)             // White text (dark mode)
val TextSecondary = Color(0xFF8B949E)           // Gray text
val TextMuted = Color(0xFF6E7681)               // Muted text
val TextPrimaryLight = Color(0xFF1A1A1A)        // Dark text (light mode)
val TextSecondaryLight = Color(0xFF757575)      // Gray text (light mode)

// === STATUS COLORS ===
val SuccessGreen = Color(0xFF3FB950)            // Green for active/success
val WarningOrange = Color(0xFFD29922)           // Orange for warnings
val ErrorRed = Color(0xFFF85149)                // Red for errors/danger
val InfoBlue = Color(0xFF58A6FF)                // Blue for info

// === WAYPOINT CATEGORY COLORS ===
val CampColor = Color(0xFF3FB950)               // Green
val WaterColor = Color(0xFF58A6FF)              // Blue
val DangerColor = Color(0xFFF85149)             // Red
val ViewpointColor = Color(0xFFA371F7)          // Purple
val ExitColor = Color(0xFFD29922)               // Orange
val CustomColor = Color(0xFF8B949E)             // Gray

// === COMPASS SPECIFIC ===
val CompassRingDark = Color(0xFF30363D)         // Ring on dark bg
val CompassRingLight = Color(0xFFE0E0E0)        // Ring on light bg
val NorthRed = Color(0xFFF85149)                // North indicator
val NeedleWhite = Color(0xFFFFFFFF)             // Needle color

// === NAVIGATION BAR ===
val NavBarDark = Color(0xFF161B22)              // Dark nav bar
val NavBarLight = Color(0xFFFFFFFF)             // Light nav bar
val NavBarSelected = Color(0xFF21262D)          // Selected item bg

// === LEGACY COMPATIBILITY ===
val DeepPurple = Color(0xFF5E35B1)
val LightPurple = Color(0xFFD1C4E9)
val CompassBg = Color(0xFFFAFAFA)
val CompassCardBg = Color(0xFFFAFAFA)
val NeedleRed = Color(0xFFF85149)
val DarkText = Color(0xFF1A1A1A)
val GrayText = Color(0xFF8B949E)
val ChartreusePrimary = Color(0xFFCDFF00)
val BluePrimary = Color(0xFF58A6FF)
val CreamBackground = Color(0xFFF5F3EE)
val WarningRed = Color(0xFFF85149)
val WarningRedLight = Color(0xFFFDECEC)
